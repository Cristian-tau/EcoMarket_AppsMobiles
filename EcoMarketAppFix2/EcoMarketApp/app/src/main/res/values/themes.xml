<!-- (1) -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- (2) -->
    <style name="Theme.EcoMarketApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- (3) -->
        <item name="colorPrimary">@color/eco_green</item>
        <!-- (4) -->
        <item name="colorPrimaryVariant">@color/eco_green_dark</item>
        <!-- (5) -->
        <item name="colorOnPrimary">@color/eco_white</item>
        <!-- (6) -->
        <item name="android:statusBarColor">@color/eco_green_dark</item>
        <!-- (7) -->
        <item name="android:navigationBarColor">@color/eco_green</item>
        <!-- (8) -->
        <item name="android:windowBackground">@color/eco_gray</item>
    </style>
</resources>